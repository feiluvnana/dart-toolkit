/// # XPath
///
/// A compiled XPath 1.0 subset over any tree that implements `XPathTree`; `html` exposes it
/// as `\$x`, `xml` as `\$`. No dependencies.
///
/// {@category Formats}
library;

part 'src/xpath/xpath.dart';
