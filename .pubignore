# What pub publishes: everything git tracks, plus the prebuilt native binaries, minus the
# Rust build tree, the tests and the tooling.
.dart_tool/
build/
coverage/
native/target/
test/
tool/
Makefile
